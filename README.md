# Sample workspace

This is a tiny fake monorepo used only for `workspace-kb` examples.

## Architecture overview

Services talk over HTTP. Prefer searching the knowledge base before opening every README.
